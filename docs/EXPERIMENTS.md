# Experiments

Only SUMO-only seed-1 scenario validation has run. No custom Veins experiments have run. The prescribed matrix is four configurations × three densities × thirty matched seeds, after all validation tests pass. Each matched comparison must reuse the exact density/seed route file, accident placement, SUMO seed, and radio settings across its four configurations.
